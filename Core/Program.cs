//using Core.Models;
using Microsoft.EntityFrameworkCore;
using System;
using System.Linq;
using System.Threading.Tasks;

namespace Core
{
    class Program
    {

        static async Task Main(string[] args)
        {

            //var compra = await Services.GadoServices.GetAll(compraGado);

            //var lstAnimal = GadoServices.GetAll();

            //foreach (var a in lstAnimal)
            //{
            //    Console.WriteLine("{0}, {1},{2}", a.Id, a.Descricao, a.Preco);
            //}


            //Console.ReadLine();
        }
    }
}
